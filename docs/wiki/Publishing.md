# Publishing

Device install: `kraken tentacle add <dir|git>` → `~/.kraken/tentacles/`.

Public list: `schemas/catalog.json` + `POST /api/kraken/catalog/register` (held until review).

Steps: `docs/PUBLISHING.md`.
