# Hooks wiki

See [HOOKS.md](../HOOKS.md) for triggers vs hooks vs middleware, cycle detection, and notify.

Quick run:

```bash
python3 bin/kraken run echo ping
python3 bin/kraken tentacle plan weather-pro
KRAKEN_NOTIFY_STDOUT=1 python3 bin/kraken run echo ping
```
