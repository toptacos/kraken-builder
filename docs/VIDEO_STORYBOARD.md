# Video storyboard — Kraken CLI

Record on a logged-in Mac with a display. This agent session cannot grab the framebuffer (`could not create image from display`).

```
KRAKEN_RECORD=1 sh scripts/chrome/capture-content.sh
```

| Time | Shot | VO |
|------|------|----|
| 0–8s | Terminal `curl \| sh` then `kraken demo` | One command. Los Angeles JSON. No account. |
| 8–16s | kraken.topta.co/cli | Install is the product. |
| 16–24s | /blog/tentacle-bash-go | bash Go Python PHP JS Node osascript. |
| 24–32s | /blog/compile-c-docker-suckers | Docker optional. Suckers stamp then tag. |
| 32–40s | Terminal `kraken instance export` | Share tentacles. Never keys. |

Stills already live: https://kraken.topta.co/social/
