# Talking-head brief (Nyx)

This sandbox cannot generate a person who blinks and talks. Stills + ffmpeg only move the camera.

## Identity plate (use this file only)

`website/img/nyx-real/now.jpg`

Do not upload desk.jpg / lab.jpg / lot.jpg unless the line is “before Kraken.”

## Voice

`website/media/nyx-scratch.mp3` (Eve, original)

Script tone: informational, a little excited about the command, not perk.

Explainer line:

> Kraken is one binary. You add tentacles. The machine stays yours. Run doctor. Add geo. License the premium arm. Chain them in a workflow.

## How to make her move

1. HeyGen or D-ID: photo avatar from `now.jpg`, audio = `nyx-scratch.mp3` or paste the line above.
2. Real camera: same haircut, same earring, navy overshirt, sit and talk the line.
3. Drop the MP4 on the left of the explainer; keep the terminal on the right.

Until that file exists, the site uses Ken Burns (`media/nyx-motion.mp4`). That is a pan, not a performance.
