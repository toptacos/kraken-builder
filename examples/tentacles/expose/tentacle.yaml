name: expose
version: "0.1.0"
runtime: binary
binary: handler.py
license: free
actions: [plan, status]
needs_docker: false
description: Plan a *.kraken.localhost name on 127.0.0.1. Never binds 0.0.0.0.
