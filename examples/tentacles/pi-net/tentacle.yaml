name: pi-net
version: "0.1.0"
license: free
runtime: binary
binary: handler.py
needs_docker: false
platforms:
  linux-amd64: { tested: true }
  linux-arm64: { tested: true }
  darwin-arm64: { tested: false }
actions: [discover, status, add]
description: Find a Raspberry Pi on the LAN (hosts list or mDNS names) or use raw SSH credentials. No Docker.
